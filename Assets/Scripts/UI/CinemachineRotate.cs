using Cinemachine;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CinemachineRotate : MonoBehaviour
{
    /*private CinemachineVirtualCamera virtualCamera;
    private CinemachineOrbitalTransposer orbitalTransposer;
    private float rotationSpeed = 100f;
    public CinemachineFreeLook freeLookCamera;

    // Start is called before the first frame update
    void Start()
    {
        //CinemachineFreeLook freeLookCamera = GetComponent<CinemachineFreeLook>();
        //orbitalTransposer = virtualCamera.GetCinemachineComponent<CinemachineOrbitalTransposer>();
    }

    // Update is called once per frame
    void Update()
    {
        //float rotationX = eventData.delta.x * rotationSpeed;  // 根据X轴位移计算旋转角度
        //float rotationY = eventData.delta.y * rotationSpeed;  // 根据Y轴位移计算旋转角度

        //freeLookCamera.m_XAxis.Value += rotationX;  // 绕X轴旋转
        //freeLookCamera.m_YAxis.Value += rotationY;
    }

    public Vector2 Input()
    {
        Vector2 rotateInput;
        rotateInput = PlayerObject.instance.input.inputActions.Player.Move.ReadValue<Vector2>();
        return rotateInput;
    }*/
}
